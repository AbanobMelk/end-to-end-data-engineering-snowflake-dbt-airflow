# end-to-end-data-engineering-snowflake-dbt-airflow
End-to-end ELT pipeline that ingests TPC-H sample data from Snowflake, transforms it through layered dbt models (staging → intermediate → facts), and orchestrates daily runs with Apache Airflow
